# 10. 品質需求

## 10.1 效率
- API 回傳時間應在 200ms 內 (不含 AI 對話)。
- 前端首頁加載時間（LCP）應在 1.5s 內。

## 10.2 可維護性
- 遵守 PEP8 Python 編碼規範。
- API 文件需自動產出於 `/docs` 路徑。

## 10.3 擴充性
- 角色權限系統設計為數值式 (`role=1, 2, 3`)，方便未來插入新的階層。
- 打卡欄位預留了午休時間，可靈活切換計算規則。
